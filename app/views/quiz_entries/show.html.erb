<% title "Results for #{@quiz.title}" %>

<div class="right">
<h3>Results for quiz: <%= @quiz.title %></h3><br />
<% @quiz.questions.each do |question| %>
  <b><%= question.text %></b><br />
  <% question.answers.each do |answer| %>
    <%= "X " if @quiz_entry.answers.include?(answer) %>
    <%= "C " if question.right_ans == answer %>
    <%= answer.text %><br />
  <% end %>
  <br />
<% end %>
You scored: <%= @quiz_entry.correct %> / <%= @quiz_entry.possible %><br />
Percentile: <%= @quiz_entry.percentile %><br /><br />

Likes: <%= @quiz.like_count %> <%= button_to "Like", {:controller => "likes", :action => "create", :id => @quiz.id} %>
Comments: <br />
<% if @quiz.comments.empty? %>
  Be the first to comment!
<% else %>
  <%= render :partial => "common/comment", :collection => @quiz.comments %>
<% end %>
<% form_for :comment, :url => {:controller => "comments", :action=>:create} do |f| %>
  <%= f.error_messages %>
  <%= hidden_field :comment, :quiz_id, :value => @quiz.id %>
  <%= f.text_area :text %><br />
  <%= f.submit "Add comment" %><br />
<% end %>
<br /></div>
<%= render :partial=>"common/quiz_info", :locals => { :quiz => @quiz } %>
